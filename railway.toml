[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx playwright install chromium --with-deps && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "web-change-monitor"
